{
  "name": "dataset-merge-dedup",
  "version": "2.0.0",
  "description": "Merge and deduplicate items from one or more Apify datasets by any field combination.",
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "start": "node main.js"
  },
  "dependencies": {
    "apify": "^3.2.6",
    "apify-client": "^2.9.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
